<!DOCTYPE html>
<html>
  <head>
    <title>CatHotwire</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flex flex-col min-h-screen">
    <%= render(Utility::FlashMessagesComponent.new()) %>
    <%= render(Navigation::HeaderComponent.new(current_path: @current_path)) %>    
    <main class="container flex-grow mx-auto mt-28 px-5">
      <%= yield %>
    </main>
    <%= render(Navigation::FooterComponent.new()) %>
  </body>
</html>
